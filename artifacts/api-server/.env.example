# Postgres connection string (Neon, Replit, Supabase, local, …).
# The server auto-creates its schema and seeds demo data on first start.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Port the API listens on.
PORT=5000
