# Base URL of the API server. When reachable, the web console runs in "online"
# mode (shared data via Postgres). Leave unset to run in offline demo mode
# (self-contained localStorage seed data).
VITE_API_URL=http://localhost:5000/api
